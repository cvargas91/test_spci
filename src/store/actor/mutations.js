export function setActores (state, actores) {
    state.actores = actores;
}

export function setActor (state, actor) {
    state.detalleActor = actor;
}

export function setDireccion(state, direcciones) {
    state.direccion = direcciones;
}

export function setUnidad (state, unidades) {
    state.unidad = unidades;
}
