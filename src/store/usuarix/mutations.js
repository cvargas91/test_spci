export function setUsuario(state, usuario) {
  state.usuario = usuario;
}
export function setOpciones(state, opciones) {
  state.opciones = opciones;
}
export function setAccessToken(state, token) {
  state.token = token;
}
